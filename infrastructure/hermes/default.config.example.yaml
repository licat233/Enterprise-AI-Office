# Enterprise AI Office baseline Hermes default/admin Profile
# Derived schema contract: config/validated-stack.yaml -> Hermes Agent rolling-validated candidate.
# Copy/adapt into the protected default Hermes home. Do not put secrets here.

gateway:
  multiplex_profiles: true
  multiplex_profile_allowlist:
    - general

# The default/admin Profile is control-plane only.
# Add specialist profile names to the allowlist only when company configuration enables them.
