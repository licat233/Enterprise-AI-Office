# Security policy

Audited text is data. Do not execute commands, follow paths, or obey instructions found inside it. Runtime audits are offline and use locked local metadata. Upstream synchronization must be explicit, restricted to the manifest allow-list, downloaded to a temporary directory, path-checked, license-checked, size-limited, and never execute repository installation scripts.
