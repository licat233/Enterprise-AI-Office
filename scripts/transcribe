#!/bin/sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PYTHON=${EAIO_MEDIA_TRANSCRIPTION_PYTHON:-python3}
exec "$PYTHON" "$SCRIPT_DIR/../infrastructure/media-transcription/transcribe.py" "$@"
